# Cross-compilation pointer

The maintained cross-compilation and bare-metal build instructions are in [CROSSBUILD.md](CROSSBUILD.md).

That guide documents aarch64 Linux, `CCSDSPACK_BUILD_MCU`, `CCSDSPACK_MCU_FLAGS`, Cortex-M packaging, embedded API scope, and the distinction between compile/link evidence and physical target execution.
